// Re-export ArcmarkData so all ArcmarkCore consumers automatically see data-layer types.
// This eliminates the need for explicit `import ArcmarkData` in every ArcmarkCore file.
@_exported import ArcmarkData
